# Frontend environment variables
# PUBLIC_INTERFACE
# REACT_APP_BACKEND_URL sets the base URL for the backend API.
# If not provided, the app attempts to auto-detect same-host:3001, then falls back to http://localhost:3001.
REACT_APP_BACKEND_URL=http://localhost:3001
